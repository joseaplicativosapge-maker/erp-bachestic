version: "3.8"
services:
  app:
    build: .
    ports:
      - "3005:3000"
    environment:
      - NODE_ENV=production
      - SESSION_SECRET=kX9mP2qL7nR4wY1vB8zA5xC3
      - DATABASE_URL=/data/bachestic.db
    volumes:
      - sqlite_data:/data
    restart: unless-stopped

volumes:
  sqlite_data: